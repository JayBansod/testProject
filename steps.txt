////connect to git repositery
//--global is used for cofigure all repositery
git config --global user.name "<user name>"
git config --global user.email "<email>"

//local
git config user.name "<user name>"
git config user.email "<email>"

////commit and push
//commit massage


-> git init: Initialize the Folder for git repo
-> git status: Get the current status of files inside initialzed folder
-> git add: Gets files into staging area
-> git commit: Use to add commits
-> git clone: Clone/download repo
 